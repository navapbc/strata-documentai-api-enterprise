{
  "name": "docai-admin-ui",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "esbuild src/main.js --bundle --outfile=dist/bundle.js --sourcemap --watch --serve=3000 --servedir=. --loader:.html=text",
    "build": "esbuild src/main.js --bundle --outfile=dist/bundle.js --minify --loader:.html=text",
    "lint": "eslint src/ tests/ e2e/",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test",
    "record": "playwright test --config=playwright.video.config.js",
    "format": "prettier --write src/ styles/ tests/ e2e/ ../shared/",
    "format:check": "prettier --check src/ styles/ tests/ e2e/ ../shared/",
    "preview": "python3 -m http.server 3000"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "esbuild": "^0.28.1",
    "eslint": "^10.8.0",
    "eslint-plugin-no-unsanitized": "^4.1.5",
    "jsdom": "^30.0.1",
    "prettier": "^3.9.6",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "chart.js": "^4.5.1",
    "qrcode": "^1.5.4"
  }
}
